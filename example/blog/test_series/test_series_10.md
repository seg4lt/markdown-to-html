```@@frontmatter
{
  "title": "Test Series 10 - Inline Formatting",
  "description": "Various inline formatting options including emphasis, code, and special characters.",
  "date": "2024-01-10",
  "index": 10
}
```

# Test Series 10 - Inline Formatting

{{ @@blog_series_toc }}

## Emphasis and Strong

This text has *italic emphasis* and **bold emphasis**.

You can also use _underscores for italic_ and __double underscores for bold__.

Combining formats: ***bold and italic*** or ___triple underscores___.

## Inline Code

Use `inline code` for technical terms like `function_name()` or `variable_name`.

Here's code with backticks: `` `code` `` inside text.

Complex inline code: `const result = await fetch('/api/data')`.

## Mixed Formatting

This sentence has **bold text**, *italic text*, and `inline code` all together.

You can have ***bold italic*** text with `code` in the same paragraph.

## Special Characters

Here are some special characters: ©, ®, ™, €, £, ¥, §, ¶.

Mathematical symbols: ±, ×, ÷, ≤, ≥, ≠, ≈, ∞.

Arrows: →, ←, ↑, ↓, ↔, ⇒, ⇐.

## Escaped Characters

Here's an asterisk \* that's not formatting.

And a backslash \\ that's displayed.

Underscores \_ that aren't formatting.

## Code in Lists

- Item with `inline code`
- Another item with `more_code()`
- Item with **bold** and `code` together

## Code in Block Quotes

> This quote contains `inline code` and **bold text**.
> It also has *italic text* with `more_code`.
